pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
jupyter>=1.0

# Optional: the project uses sklearn's HistGradientBoostingRegressor
# (algorithmically the same family as XGBoost/LightGBM) because xgboost
# could not be installed in the offline build environment. To use real
# XGBoost instead, uncomment the line below and see src/forecasting.py.
# xgboost>=2.0
